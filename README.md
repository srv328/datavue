# DataVue

[![GitHub](https://img.shields.io/badge/GitHub-srv328-blue)](https://github.com/srv328)

Современная система управления данными с ролевой моделью доступа, аналитикой и экспортом. Администраторы создают справочники с настраиваемыми полями, управляют пользователями и правами доступа, а пользователи вводят данные, анализируют их с помощью графиков и статистики, и экспортируют в различных форматах.

## 🚀 Основные возможности

### 👨‍💼 Для администраторов:

- **Управление типами данных** - создание справочников с настраиваемыми полями различных типов
- **Управление пользователями** - создание, редактирование и удаление пользователей системы
- **Система разрешений** - гибкая настройка прав доступа к данным по таблицам (чтение/запись)
- **Управление записями** - полный доступ к редактированию и удалению записей данных
- **Типы данных:**
  - Текст
  - Целое число
  - Десятичное число
  - Дата
  - Булево значение
  - **Перечислимое значение (enum)** - выпадающий список с заданными администратором значениями
  - **Координаты** - широта и долгота с валидацией

### 👤 Для пользователей:

- **Просмотр данных** - доступ ко всем типам данных для анализа
- **Ввод записей** - добавление записей в разрешенные справочники
- **Анализ данных** - статистический анализ с графиками и диаграммами:
  - Простой анализ данных в виде карточек со статистикой
  - Интерактивные графики (столбчатые, линейные)
  - Гибкая таблица данных с поиском, сортировкой и выбором колонок
  - Статистические показатели: среднее, минимум, максимум, медиана, сумма
- **Экспорт данных** - выгрузка в CSV и Excel с настройками:
  - Выбор конкретных колонок для экспорта
  - Настройка количества записей
  - Включение/исключение заголовков и описаний

### 🔧 Технические особенности:

- **Современный стек** - Vue.js 3 + Flask + SQLite
- **Адаптивный дизайн** - работает на всех устройствах
- **Темная/светлая тема** - переключение темы интерфейса
- **Ролевая модель** - гибкая система прав доступа
- **Экспорт данных** - CSV и Excel с выбором колонок
- **Валидация данных** - проверка типов и обязательных полей
- **Гибкая таблица** - поиск, сортировка, пагинация, выбор отображаемых колонок

## 📦 Установка и запуск

### Предварительные требования

- **Python** 3.8+ (рекомендуется 3.10+)
- **Node.js** 20.17.0+ (проверьте версию: `node --version`)
- **npm** или **yarn**
- **Git** (для клонирования репозитория)

### 1. Клонирование репозитория

```bash
git clone https://github.com/srv328/datavue.git
cd datavue
```

### 2. Установка зависимостей

#### Python (бэкенд)

Рекомендуется использовать виртуальное окружение:

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

Установка зависимостей:

```bash
pip install -r requirements.txt
```

#### Node.js (фронтенд)

```bash
npm install
```

### 3. Запуск приложения

Приложение состоит из двух частей, которые нужно запускать одновременно:

#### Запуск бэкенда (терминал 1)

```bash
# Активируйте виртуальное окружение (если используете)
python server.py
```

Сервер запустится на `http://localhost:5000`

#### Запуск фронтенда (терминал 2)

```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

### 4. Первый запуск

При первом запуске система покажет форму настройки администратора. Создайте учетную запись администратора с именем пользователя и паролем.

**Примечание:** После создания администратора вы автоматически войдете в систему.

## 🎯 Быстрый старт

### 1. Первый вход

- При первом запуске создайте учетную запись администратора
- Система автоматически войдет с созданными учетными данными
- Перейдите во вкладку "Администрирование" для управления системой

### 2. Создание типа данных

1. Перейдите во вкладку **"Администрирование"**
2. В разделе **"Типы данных"** нажмите "Добавить тип данных"
3. Введите название и описание типа данных
4. Добавьте поля различных типов:
   - **Текст** - для строковых значений
   - **Целое число** - для целых чисел
   - **Десятичное число** - для дробных чисел
   - **Дата** - для дат
   - **Булево значение** - для да/нет
   - **Перечислимое значение** - введите возможные значения через запятую (они появятся в выпадающем списке)
   - **Координаты** - для географических координат (широта и долгота)
5. Отметьте поля как обязательные, если необходимо
6. Сохраните тип данных

### 3. Управление пользователями

1. Во вкладке **"Администрирование"** перейдите в раздел **"Пользователи"**
2. Нажмите "Добавить пользователя"
3. Заполните данные пользователя (имя, пароль, роль)
4. В разделе **"Права доступа"** выдайте разрешения пользователю на редактирование конкретных типов данных

### 4. Работа с данными

1. Перейдите на вкладку **"Данные"** для ввода записей:
   - Выберите тип данных
   - Заполните форму и нажмите "Добавить запись"
   - Для администраторов доступны кнопки редактирования и удаления записей

2. Используйте вкладку **"Анализ"** для просмотра:
   - Простого анализа данных в виде карточек
   - Статистики по полям
   - Интерактивных графиков
   - Гибкой таблицы данных с поиском и сортировкой
   - Экспорта данных в CSV или Excel

### 5. Экспорт данных

1. На вкладке **"Анализ"** выберите тип данных
2. Нажмите кнопку **"Экспорт в CSV"** или **"Экспорт в Excel"**
3. В модальном окне:
   - Выберите колонки для экспорта (можно выбрать все или конкретные)
   - Установите количество записей (0 = все записи)
   - Включите/исключите заголовки и описания
4. Нажмите "Экспортировать" для скачивания файла

## 🛠️ Утилиты

### Сброс пароля администратора

Если вы забыли пароль администратора, используйте утилиту для его сброса:

```bash
python reset_admin_password.py
```

Утилита предложит:
- Сброс на пароль по умолчанию (`admin`)
- Ввод нового пароля
- Просмотр информации о текущем администраторе

## 📊 Демонстрационные данные

В проекте доступен скрипт для загрузки демонстрационных данных:

```bash
python load_boston_dataset.py
```

Это загрузит Boston Housing Dataset с 506 записями для демонстрации возможностей аналитики и экспорта.

## 🗄️ Структура базы данных

Система использует SQLite базу данных (`datavue.db`) со следующими таблицами:

### Основные таблицы

- **`users`** - пользователи системы с ролями (admin/user) и профилями
- **`data_types`** - типы данных (справочники) с метаданными (название, описание)
- **`data_fields`** - поля справочников с типами и валидацией (обязательность, описание)
- **`enum_field_values`** - значения для полей типа "enum" (перечислимые значения)
- **`user_permissions`** - разрешения пользователей на чтение/запись данных

### Динамические таблицы

- **`data_{id}`** - таблицы для хранения записей данных (создаются автоматически для каждого типа данных)

## 🔧 Технический стек

### Backend

- **Flask 2.3.3** - веб-фреймворк
- **SQLite** - база данных
- **openpyxl 3.1.5** - работа с Excel файлами
- **Flask-CORS 4.0.0** - поддержка CORS для API

### Frontend

- **Vue.js 3.3.4** - реактивный фреймворк
- **Vite 4.5.2** - сборщик и dev-сервер
- **Chart.js 4.5.1** - библиотека для графиков и диаграмм
- **vue-chartjs 5.3.2** - интеграция Chart.js с Vue
- **Tailwind CSS** - утилитарная CSS-библиотека для стилизации

## 📁 Структура проекта

```
datavue/
├── src/
│   ├── components/          # Vue компоненты
│   │   ├── AdminPanel.vue   # Панель администратора
│   │   ├── AnalysisPanel.vue # Панель анализа данных
│   │   ├── DataManagement.vue # Управление данными
│   │   ├── ConfirmDialog.vue  # Диалог подтверждения
│   │   ├── ModalSystem.vue     # Система модальных окон
│   │   └── NotificationSystem.vue # Система уведомлений
│   ├── assets/              # CSS и стили
│   ├── main.js              # Точка входа Vue приложения
│   └── App.vue              # Главный компонент
├── database.py              # Менеджер базы данных
├── server.py                # Flask сервер
├── reset_admin_password.py  # Утилита сброса пароля
├── load_boston_dataset.py   # Скрипт загрузки демо-данных
├── requirements.txt         # Python зависимости
├── package.json             # Node.js зависимости
└── vite.config.js           # Конфигурация Vite
```

## 🚧 Разработка

### Запуск в режиме разработки

```bash
# Бэкенд с автоматической перезагрузкой
python server.py

# Фронтенд с hot-reload
npm run dev
```

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

### Запуск продакшен-версии

Настройте веб-сервер (например, nginx) для обслуживания статических файлов из `dist/` и проксирования API запросов на Flask сервер.
